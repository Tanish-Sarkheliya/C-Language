#include<stdio.h>
int main()
{
    int n, ld, fd, sum=0;
    printf("Enter N: ");
    scanf("%d", &n);
    do(n!=0);
    {
        ld=n%10;
        fd=
        sum=ld+fd;                                           
    }while(n==0);
    printf("Sum of last two digits:%d", sum);
}